server:
  port: 8083

spring:
  application:
    name: provider-service

  ###############################################
  # PostgreSQL (R2DBC - Reactive)
  ###############################################
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/provider_db
    username: admin
    password: 123456

  sql:
    init:
      mode: never

logging:
  level:
    org.springframework.r2dbc: DEBUG
